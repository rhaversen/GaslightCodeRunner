import { MeyerStrategyAPI } from '../../meyer/types.ts'

export default function (api: MeyerStrategyAPI) {
    const x = [1, 2, 3;    // Syntax error: missing bracket
    if (true) {            // Syntax error: missing closing brace
    return api.roll()      // Syntax error: missing semicolon
    const y = 42;          // Unreachable code
}
