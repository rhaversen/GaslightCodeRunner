# Environment.
NODE_ENV=development

# Which runner entrypoint to boot: "evaluation" (service) or "tournament"
# (daily batch). Required in all environments (the dev entrypoint sets
# "evaluation" if unset).
RUNNER_MODE=evaluation

# Shared token for service-to-service authentication. Required in all
# environments (the dev entrypoint sets a TEST_ value if unset).
MICROSERVICE_AUTHORIZATION=

# Sentry error tracking. Optional; unset disables Sentry.
SENTRY_DSN=

# BetterStack log ingestion token. Required when NODE_ENV=production or
# staging. Unused in development/test.
BETTERSTACK_LOG_TOKEN=

# Per-source ingestion host from the Better Stack dashboard (e.g.
# s762702.eu-central-1a.betterstackdata.com). Optional: omit to use the
# client default (in.logs.betterstack.com).
BETTERSTACK_INGESTING_HOST=
